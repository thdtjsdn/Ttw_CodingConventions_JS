//----------------------------------------------------------------------------------------------------;
var fileNm = "템플릿_클래스.js";
if( console ) console.log( "[ S ] - " + fileNm + "----------" );
//----------------------------------------------------------------------------------------------------;

//----------------------------------------------------------------------------------------------------;

//	Package;

//----------------------------------------------------------------------------------------------------;

window.b2link = window.b2link || {};
window.b2link.scm = window.b2link.scm || {};

//----------------------------------------------------------------------------------------------------;

//	CLASS - window.b2link.scm.QueryBD;

//----------------------------------------------------------------------------------------------------;

/**
 * @class QueryBD
 * @param {Object} d data
 * <code>
 * {
 * }
 * </code>
 *
 * @return {window.b2link.scm.QueryBD}
 * <code>
 * {
 * }
 * </code>
 *
 * @example
 * window.b2link_SCM_QueryBD = new window.b2link.scm.QueryBD( {} );
 *
 * @Dependency window.b2link.util
 */
window.b2link.scm.QueryBD = function( d )
{
	/**
	 * @private
	 * @Property {String}
	 */
	var _nmInstance = "";

	/**
	 * @private
	 * @Property {String}
	 */
	var _nmPackage = "window.b2link.scm.QueryBD";

	window.console.timeStamp( "-------[ S ] - " + _nmPackage + "():" + _nmPackage );

	//----------------------------------------------------------------------------------------------------;

	//	STATIC;

	//----------------------------------------------------------------------------------------------------;

	//--------------------------------------------------PUBLIC;

	//--------------------------------------------------PROTECTED;

	//--------------------------------------------------PRIVATE;

	//----------------------------------------------------------------------------------------------------;

	//	MEMBER INSTANCE;

	//----------------------------------------------------------------------------------------------------;

	//--------------------------------------------------PUBLIC;

	//--------------------------------------------------PROTECTED;

	//--------------------------------------------------PRIVATE;


	//----------------------------------------------------------------------------------------------------;

	//	EVENT;

	//----------------------------------------------------------------------------------------------------;

	//----------------------------------------------------------------------------------------------------;

	//	FUNCTION;

	//----------------------------------------------------------------------------------------------------;

	//----------------------------------------------------------------------------------------------------;

	//	GETTER / SETTER;

	//----------------------------------------------------------------------------------------------------;

	//--------------------------------------------------GET Method;

	//--------------------------------------------------SET Method;

	//--------------------------------------------------GET && SET Method;

	//--------------------------------------------------GETTER Method;

	//--------------------------------------------------SETTER Method;

	//--------------------------------------------------GETTER && SETTTER Method;

	window.console.timeStamp( "-------[ E ] - " + _nmPackage + "():" + _nmPackage );

	return this;
};

//----------------------------------------------------------------------------------------------------;
if( console ) console.log( "[ E ] - " + fileNm + "----------" );
//----------------------------------------------------------------------------------------------------;